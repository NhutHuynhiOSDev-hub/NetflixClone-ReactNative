import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
  },
  image: {
    width: "100%",
    aspectRatio: 16/9,
    resizeMode: "cover",
  },
});

export default styles;
